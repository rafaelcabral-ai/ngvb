% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotfunctions.R
\name{areal.plot}
\alias{areal.plot}
\title{Plot areal (regional) data on a map.}
\usage{
areal.plot(map, data, palette = "YlOrRd", title = "Value")
}
\arguments{
\item{map}{An `sf` object with one row per region (its geometry column is used).}

\item{data}{Numeric vector, one value per region, in the row order of `map`.}

\item{palette}{A `RColorBrewer` sequential palette name; the default `"YlOrRd"`
shades low values yellow and high values red.}

\item{title}{Legend title.}
}
\value{
A `ggplot` object.
}
\description{
Plot areal (regional) data on a map.
}
\examples{
\donttest{
if (requireNamespace("sf", quietly = TRUE)) {
  nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
  areal.plot(nc, nc$BIR74, title = "births")
}
}
}
