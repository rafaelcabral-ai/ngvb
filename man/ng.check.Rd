% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ng_check.R
\name{ng.check}
\alias{ng.check}
\title{Check the latent Gaussian assumption of an INLA fit.}
\usage{
ng.check(fit, selection = NULL, components = NULL, compute.fixed = TRUE)
}
\arguments{
\item{fit}{An `inla` object fitted with `control.compute = list(config = TRUE)`.}

\item{selection}{Optional named list of components to check (default: all random).}

\item{components}{Optional operator overrides (e.g. SPDE), as in [ngvb()].}

\item{compute.fixed}{If `TRUE`, also return the sensitivity of each fixed effect
to each component's non-Gaussianity parameter.}
}
\value{
An object of class `ngvb.check`: per component the BF sensitivity `s0`,
  the per-index contributions `d`, and (Gaussian response) the reference SD and
  p-value; plus `sens.fixed` if requested.
}
\description{
Check the latent Gaussian assumption of an INLA fit.
}
