% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{ngvb_svi}
\alias{ngvb_svi}
\title{Fit a latent non-Gaussian model by structured variational inference.}
\usage{
ngvb_svi(
  inla.fit.V,
  op,
  comp.name,
  alpha.eta = 1,
  iter = 10,
  stop.rel.change = 0.001,
  n.sampling = 2000,
  verbose = TRUE
)
}
\arguments{
\item{inla.fit.V}{Function mapping a list of mixing vectors `V` to an INLA
fit of the LGM conditioned on `V` (must use `control.compute=list(config=TRUE)`).}

\item{op}{Operator descriptor (provides `Dfunc`, `h`).}

\item{comp.name}{Name of the latent component (the `f(<name>, ...)` index).}

\item{alpha.eta}{Rate of the exponential PC prior on the non-Gaussianity
parameter eta.}

\item{iter}{Maximum number of VB iterations.}

\item{stop.rel.change}{Stop when the relative change in E[eta] drops below this.}

\item{n.sampling}{Monte-Carlo samples for the eta moments.}

\item{verbose}{Print per-iteration progress.}
}
\value{
A list with the final INLA fit, the mixing vector `V`, `eta`, the
  discrepancy `d`, and the `eta` trajectory.
}
\description{
Fit a latent non-Gaussian model by structured variational inference.
}
