% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{bayes.factor}
\alias{bayes.factor}
\alias{bayes.factor.ngvb}
\alias{bayes.factor.ngvb.samples}
\title{Bayes factor of a latent non-Gaussian model against its Gaussian counterpart.}
\usage{
bayes.factor(x, ...)

\method{bayes.factor}{ngvb}(x, ...)

\method{bayes.factor}{ngvb.samples}(x, ...)
}
\arguments{
\item{x}{An `ngvb` object (sampled internally) or an `ngvb.samples` object.}

\item{...}{For the `ngvb` method, passed to [ngvb_sample()] (e.g. `n.samples`, `seed`).}
}
\value{
A list with `BF`, `log10BF`, `logBF`, and the effective sample size
  `ess` of the importance weights (a low `ess` means more draws are needed).
}
\description{
Returns the marginal-likelihood Bayes factor \eqn{Z_{\mathrm{LnGM}}/Z_{\mathrm{LGM}}}
with the mixing variables `V` integrated out by importance sampling from
`q(V)`. A value above one favours the non-Gaussian model; the usual Jeffreys
reading is that \eqn{\log_{10}} above 0.5, 1, 2 is substantial, strong, decisive.
}
