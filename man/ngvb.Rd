% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{ngvb}
\alias{ngvb}
\title{Fit a latent non-Gaussian model from a fitted INLA (LGM) object.}
\usage{
ngvb(
  fit,
  selection = NULL,
  components = NULL,
  method = c("SCVI", "SVI"),
  alpha.eta = 1,
  iter = 10,
  stop.rel.change = 0.001,
  n.sampling = 2000,
  verbose = TRUE
)
}
\arguments{
\item{fit}{An `inla` object (fitted with `control.compute = list(config = TRUE)`).}

\item{selection}{Optional named list of component indices to extend to
non-Gaussianity (default: all random effects).}

\item{components}{Optional named list of operator descriptors overriding
auto-detection (required for SPDE: `list(s = ngvb_operator("spde", spde = spde))`).}

\item{alpha.eta}{Exponential-PC-prior rate(s) on the non-Gaussianity parameter(s).}

\item{iter, stop.rel.change, n.sampling, verbose}{VB controls.}
}
\value{
A list with the final `fit`, mixing vectors `V`, `eta`, etc.
}
\description{
Fit a latent non-Gaussian model from a fitted INLA (LGM) object.
}
